#ifndef WORD_SEQUENCE_H_ALREADY_INCLUDED
#define WORD_SEQUENCE_H_ALREADY_INCLUDED

#pragma once

#include <iostream>
#include <string>

std::string getUniqueWords(
    std::istream& input);

std::string getSortedUniqueWords(
    std::istream& input);

#endif